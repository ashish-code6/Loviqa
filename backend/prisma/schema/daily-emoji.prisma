model DailyEmoji {
  id     String @id @default(uuid())
  userId String
  emoji  String
  date   String

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, date])
}
